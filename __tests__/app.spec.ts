import * as express from '../src/App';

describe('inicia server', () => {

  beforeAll(async () => {
   // express;
  });

  it('delays the greeting by 2 seconds', () => {
    console.log('teste');
  });

});
